# Programming 소스 코드 🧐
## 02_Counter
Vanilla HTML, JavaScript

## React 시작 😜
```shell
npm create vite@latest .
```

### CounterApp 😎
- `useStat(초기값)`
- `onClick={() => set함수(바꿀 값}`
- `onClick={() => set함수((이전 state) => (이전 state + 1)}`
- `onClick={함수 이름}`

### TodoListApp 🗓️


